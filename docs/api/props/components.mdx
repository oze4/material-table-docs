---
id: mt_prop_components
title: components
---

import MaterialTable from 'material-table';
import tableIcons from '../../../components/MaterialTable/tableIcons';

## The `components` Property

:::note <h3 style={{display:'inline-block'}} id="intro">Intro</h3>
 - The `components` property accepts an object [with the following fields/shape](#shape)
   - More info on each [field can be found here](#fields)
 - Each field within the `components` object expects a function that returns a `React.Element` type
   - We pass a [props](#link-to-more-about-the-props-object-in-callback) object to said function
:::

### Shape

:::info Optional
These fields are all optional
:::

```jsx
// See more HERE on the `(props)` object that gets passed
const components = {
  Action: (props) => <React.Element />,
  Actions: (props) => <React.Element />,
  Body: (props) => <React.Element />,
  Cell: (props) => <React.Element />,
  Container: (props) => <React.Element />,
  EditField: (props) => <React.Element />,
  EditRow: (props) => <React.Element />,
  FilterRow: (props) => <React.Element />,
  Groupbar: (props) => <React.Element />,
  Header: (props) => <React.Element />,
  OverlayLoading: (props) => <React.Element />,
  Pagination: (props) => <React.Element />,
  Row: (props) => <React.Element />,
  Toolbar: (props) => <React.Element />,
}
```

### Fields

<div>
    <MaterialTable
    title="Components Property - Fields"
    options={{
        maxBodyHeight: 400,
        pageSize: 14
    }}
    icons={tableIcons}
    columns={[
        { field: 'field', title: 'Field' },
        { field: 'type', title: 'Type' },
        { field: 'default', title: 'Default Component' },
        { field: 'description', title: 'Description' }
    ]}
    data={[
        {
        field: 'Action',
        type: "(props) => 'React.Element'",
        default: 'MTableAction',
        description: 'Action component'
        },
        {
        field: 'Actions',
        type: "(props) => 'React.Element'",
        default: 'MTableActions',
        description: 'Actions component'
        },
        {
        field: 'Body',
        type: "(props) => 'React.Element'",
        default: 'MTableBody',
        description: 'Body component'
        },
        {
        field: 'Cell',
        type: "(props) => 'React.Element'",
        default: 'MTableCell',
        description: 'Cell component'
        },
        {
        field: 'Container',
        type: "(props) => 'React.Element'",
        default: 'Paper',
        description: 'Container that everything renders in'
        },
        {
        field: 'EditField',
        type: "(props) => 'React.Element'",
        default: 'MTableEditField',
        description: 'EditField component'
        },
        {
        field: 'EditRow',
        type: "(props) => 'React.Element'",
        default: 'MTableEditRow',
        description: 'EditRow component'
        },
        {
        field: 'FilterRow',
        type: "(props) => 'React.Element'",
        default: 'MTableFilterRow',
        description: 'FilterRow component'
        },
        {
        field: 'Groupbar',
        type: "(props) => 'React.Element'",
        default: 'MTableGroupbar',
        description: 'FilterRow component'
        },
        {
        field: 'Header',
        type: "(props) => 'React.Element'",
        default: 'MTableHeader',
        description: 'Header component'
        },
        {
        field: 'OverlayLoading',
        type: "(props) => 'React.Element'",
        default: 'CircularProgress',
        description: 'Overlay loading component'
        },
        {
        field: 'Pagination',
        type: "(props) => 'React.Element'",
        default: 'MTablePagination',
        description: 'Pagination component'
        },
        {
        field: 'Row',
        type: "(props) => 'React.Element'",
        default: 'MTableBodyRow',
        description: 'Row component'
        },
        {
        field: 'Toolbar',
        type: "(props) => 'React.Element'",
        default: 'MTableToolbar',
        description: 'Toolbar component'
        }
    ]}
    />
</div>

## Callback Function

more on `Toolbar: props => {}` (the props object)